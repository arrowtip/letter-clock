#include "firmware/clock_board.hpp"
#include "util/timestamp.hpp"
#include <Arduino.h>

void setup() {
  ClockBoard::init();

}

void loop() {

    // get time

    // check if update necessary

    // sleep a bit
}
